import { SupportFormCard } from '@/components/support-form-card/SupportFormCard';

export default async function SupportPage() {
  return (
    <div className="pt-2 sm:mx-2">
      <SupportFormCard />
    </div>
  );
}
